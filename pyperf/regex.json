{
    "overlapChecking": ".*for (?P<first>.*) .* .*:\n.*for (?P<second>.*) .* .*\n.*if.*(?P=first) (is|==) (?P=second):.*",
    "whileTrue": ".*while (T|t)rue:.*",
    "plusEquals": ".*\".*\".*\\+=.*\".*\".*$"
}
